Public Sub-NET + VM + Public IP --> SSH -->> ping google.com -->> working

Private Sub-NET + VM + Public IP --> SSH -->> ping google.com -->> working

Private Sub-NET + VM + No Public IP --> SSH via Public VM -->> ping google.com -->> java installation didn't work

Private Sub-NET + NAT Gateway + VM + No Public IP --> SSH via Public VM -->> worked java installation
